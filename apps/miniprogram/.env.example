# Copy the selected endpoint into .env.production before an experience/release build.
# Keep this empty until your HTTPS domain and WeChat server allowlist are ready.
TARO_APP_API_BASE_URL=""
# Example shape only: https://your-api-domain/public-security/api
